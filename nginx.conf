server {
    listen ${PORT:-8080};
    server_name _;
    root /var/www/html/public;
    index index.php;
    client_max_body_size 20m;

    location ~ /build/.*\.php$ {
        deny all;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }
}
